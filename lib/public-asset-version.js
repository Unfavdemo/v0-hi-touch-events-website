/**
 * Bump when replacing images in-place under `public/` (Hitouch heroes, featured-work
 * galleries) so browsers and CDNs fetch the new bytes. Rebuild after changing.
 */
export const PUBLIC_ASSET_VERSION = "20260516"
